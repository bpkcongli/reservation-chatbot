# Backend

FastAPI modular-monolith baseline for the reservation chatbot.

Run commands from the repository root with `make dev-backend`, `make migrate`,
or the quality commands documented in the root README.
